<header>
  <nav class="navbar navbar-expand-md navbar-light fixed-top bg-light shadow">
    <div class="container-fluid">
      <a class="navbar-brand enseigne" href="{{path('home')}}">L'Aiguille Du Marquiste</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item {% if app.request.attributes.get('_route') starts with 'tatouage' %}active{% endif %}">
            <a class="nav-link" href="{{path('tatouage')}}">Réalisations</a>
          </li>
          <li class="nav-item {% if app.request.attributes.get('_route') starts with 'projet' %}active{% endif %}">
            <a class="nav-link" href="{{path('projet')}}">Projets</a>
          </li>
          <li class="nav-item {% if app.request.attributes.get('_route') starts with 'illustration' %}active{% endif %}">
            <a class="nav-link" aria-current="page" href="{{path('illustration')}}">Illustration</a>
          </li>
        </ul>
        <div class="ml-auto">
          <button class="btn btn-outline-dark" type="submit"><a class="contact-a" href="{{path('contact')}}">Contact</a></button>
        </div>
      </div>
    </div>
  </nav>
</header>